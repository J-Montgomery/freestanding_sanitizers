{
    "signed_addition_overflow": {
        "output1": "signed integer overflow: [0-9]+ \\+ 1 cannot be represented in type"
    },

    "unsigned_addition_overflow": {
        "output1": "unsigned integer overflow: [0-9]+ \\+ 1 cannot be represented in type"
    },

    "shift_overflow": {
        "output1": "HandleShiftOutOfBoundsImpl"
    },

    "test_death_override": {
        "output1": "store to null pointer",
        "output2": "^Custom Death Message$"
    }
}
